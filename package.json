{
  "name": "root-project",
  "scripts": {
    "dev": "npm-run-all --parallel start-host start-remote",
    "start-host": "cd host && npm run dev",
    "start-remote": "cd remote && npm run build && npm run preview"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}